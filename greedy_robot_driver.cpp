#include "greedy_robot.h"

using namespace std;

int main(int argc, char* argv[]) {

	/*
	if (argc == 6) {

		int max = atoi(argv[1]);
		int Rx = atoi(argv[2]);
		int Ry = atoi(argv[3]);
		int Tx = atoi(argv[4]);
		int Ty = atoi(argv[5]);

		GreedyRobot g1(max, Rx, Ry, Tx, Ty);
	}
	else {
		cout << "too few or too many arguments. Retry." << endl;
	}
	*/


	//test cases
	/*
	GreedyRobot g2(2, 1, 2, 3, 5);

	GreedyRobot g3(10, 2, 2, 2, 4);

	GreedyRobot g4(3, -1, -2, -5, -4);

	GreedyRobot g5(0, 1, 1, 1, 1);

	GreedyRobot g6(10, 2, 2, 2, 20);

	GreedyRobot g7(2, 10, -1, 5, -9);

	GreedyRobot g9(3, 5, 0, 10, 0); */

	GreedyRobot g(1, 10, 10, 1, 1);



}